const firstNumber = +prompt("Enter jour first number");
const secondNumber = +prompt("Enter Jour second number");
const result = firstNumber >= secondNumber
    ? firstNumber
    : secondNumber;
console.log(result);